extends ../../layout/default
include ../../mixins/filter-status
include ../../mixins/search
include ../../mixins/pagination

block main
    h2.inner-content #{pageTitle}

    .card.mb-3
        .card-header Filters and Search
        .card-body
            .row
                .col-6
                    +filter-status(filterStatus)
                .col-6
                    +search(title)

    .card.mb-3
        .card-header List of products
        .card-body
            table.table.table-hover.table-sm
                thead
                    tr(class="inner-content")
                        th No
                        th Picture
                        th Title
                        th Author
                        th Price
                        th Status
                        th Act
                each item, index in products
                    tbody
                        tr
                            td(class="inner-content") #{index + 1}
                            td
                                img(
                                    src=item.thumbnail
                                    alt=item.title
                                    width="50px"
                                    height="auto"
                                    style="border-radius: 10%"
                                )
                            td(class="inner-content") #{item.title}
                            td(class="inner-content") #{item.author}
                            td(class="inner-content") #{item.price}$
                            td
                                if(item.status === "active")
                                    a(href="#" class="btn btn-custom status-active") Active
                                else
                                    a(href="#" class="btn btn-custom status-unactive") Inactive
                            td
                                button.change(class="btn btn-custom edit") Modify
                                button.delete(class="btn btn-custom delete") Delete

    +pagination(pagination)